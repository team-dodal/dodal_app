import 'package:flutter/material.dart';

class ManageFeedScreen extends StatelessWidget {
  const ManageFeedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('feed'),
    );
  }
}
