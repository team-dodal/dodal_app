import 'package:flutter/material.dart';

class ModifyUserScreen extends StatelessWidget {
  const ModifyUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('유저 수정')),
    );
  }
}
